# Alt-staging ops scripts env
#
# Copy to ops/alt-staging/.env and fill in values.
#
# These scripts are designed to be:
# - minimal
# - self-contained under ops/
# - safe to delete/move later

# =========================
# Cluster
# =========================
# RPC URL for the "alt staging" cluster
ALT_STAGING_RPC_URL=

# =========================
# Program deployment
# =========================
# Program address you want to deploy/upgrade on alt staging
ALT_STAGING_PROGRAM_ID=

# Path to keypair that is the *upgrade authority* for ALT_STAGING_PROGRAM_ID
ALT_STAGING_UPGRADE_AUTHORITY_KEYPAIR=

# Optional: separate payer for deployment (defaults to upgrade authority)
ALT_STAGING_DEPLOY_PAYER_KEYPAIR=

# Optional: explicit path to the built .so (defaults to auto-detect target/deploy/*.so)
ALT_STAGING_PROGRAM_SO_PATH=

# Optional: Cargo/Anchor features used to build for alt staging.
# The feature name must match id-crate/Cargo.toml features.
# Valid values: mainnet-beta, staging, stagingalt, devnet
# Default: stagingalt (program ID: 5UDghkpgW1HfYSrmEj2iAApHShqU44H6PKTAar9LL9bY)
ALT_STAGING_BUILD_FEATURES=stagingalt

# =========================
# IDL / client
# =========================
# Path to the Anchor IDL json for your program.
# For an Anchor workspace this is usually: target/idl/<program>.json
ALT_STAGING_IDL_PATH=target/idl/marginfi.json

# =========================
# Admin + user wallets
# =========================
# Admin wallet used for group/bank admin instructions.
# (Often same as deploy payer / upgrade authority on staging.)
ALT_STAGING_ADMIN_KEYPAIR=

# Optional user wallet for init-user script
ALT_STAGING_USER_KEYPAIR=

# =========================
# Stateful values (filled after running init scripts)
# =========================
# Marginfi group (pool) pubkey. If set, scripts can omit --group arg.
ALT_STAGING_GROUP=

# Global fee wallet pubkey (optional). If unset, defaults to admin pubkey.
ALT_STAGING_GLOBAL_FEE_WALLET=

# Optional: global fee admin pubkey (defaults to admin wallet pubkey)
ALT_STAGING_GLOBAL_FEE_ADMIN=

# =========================
# Fee-state defaults (only used by init-global-fee-state.ts)
# =========================
# If you don't care about fees on alt staging, leaving these as 0 is fine.
ALT_STAGING_BANK_INIT_FLAT_SOL_FEE_LAMPORTS=0
ALT_STAGING_LIQUIDATION_FLAT_SOL_FEE_LAMPORTS=0

# Percent values encoded as I80F48 (decimal strings are OK)
ALT_STAGING_PROGRAM_FEE_FIXED=0
ALT_STAGING_PROGRAM_FEE_RATE=0
ALT_STAGING_LIQUIDATION_MAX_FEE=0
